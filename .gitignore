# Node / Playwright
node_modules/
package-lock.json

# 로그인·수집 테스트 디버그 산출물 (스크린샷 등 개인정보 포함 가능 → 커밋 금지)
sync/_debug/

# Claude Code 개인 설정 (커밋 제외)
.claude/settings.local.json
blackban_weekly_v1.html

# 오퍼스 서브에이전트 작업 사본 (git worktree) — 커밋 제외
.claude/worktrees/

# 해설집 렌더 검증용 로컬 MathJax 사본 (npm pack mathjax@3.2.2 → es5/tex-svg.js)
# 브라우저 검증 때만 쓰는 외부 라이브러리 2MB — 저장소에 두지 않는다
_mjlocal/
